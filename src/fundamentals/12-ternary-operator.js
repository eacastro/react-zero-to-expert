// ===============================
// Operador condicional ternario
// ===============================

const active = true;
// let message = '';

// if (active) {
//     message = 'Active User';
// } else {
//     message = 'Inactive User';
// }

// const message = (active) ? 'Active User' : 'Inactive User';
const message = active && 'Activo';

console.log(message);